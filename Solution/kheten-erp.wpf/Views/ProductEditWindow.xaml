<Window x:Class="kheten_erp.wpf.Views.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        Title="Ürün" Width="480" SizeToContent="Height"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Surface}">
    <Grid Margin="24,20,24,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock x:Name="HeaderTitle" Text="Yeni Ürün" Style="{StaticResource H1}" FontSize="19"/>
                <TextBlock Text="Ürün kartı bilgilerini girin" Style="{StaticResource Muted}" Margin="0,2,0,0"/>
            </StackPanel>
            <Button Style="{StaticResource GridIconButton}" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Click="Cancel_Click" ToolTip="Kapat">
                <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource FgMuted}" Data="{StaticResource Icon.Close}"/>
            </Button>
        </Grid>

        <!-- Form -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Ürün Kodu" Style="{StaticResource FieldLabel}"/>
            <TextBox Style="{StaticResource InputBox}" Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Ürün Adı" Style="{StaticResource FieldLabel}" Margin="0,14,0,6"/>
            <TextBox Style="{StaticResource InputBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <Grid Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Kategori" Style="{StaticResource FieldLabel}"/>
                    <ComboBox Style="{StaticResource Combo}" SelectedItem="{Binding Category, Mode=TwoWay}">
                        <sys:String>Hidrolik</sys:String>
                        <sys:String>Metal</sys:String>
                        <sys:String>Bağlantı</sys:String>
                        <sys:String>Elektrik</sys:String>
                        <sys:String>Yedek</sys:String>
                        <sys:String>Tesisat</sys:String>
                        <sys:String>Genel</sys:String>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Birim" Style="{StaticResource FieldLabel}"/>
                    <ComboBox Style="{StaticResource Combo}" SelectedItem="{Binding Unit, Mode=TwoWay}">
                        <sys:String>Adet</sys:String>
                        <sys:String>m²</sys:String>
                        <sys:String>m</sys:String>
                        <sys:String>Set</sys:String>
                        <sys:String>kg</sys:String>
                        <sys:String>Litre</sys:String>
                        <sys:String>Paket</sys:String>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <Grid Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Stok Miktarı" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding Stock, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Birim Fiyat" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>

            <Border CornerRadius="8" Background="{DynamicResource SurfaceAlt}" Padding="12,9" Margin="0,14,0,0">
                <StackPanel Orientation="Horizontal">
                    <Path Width="15" Height="15" Stretch="Uniform" Fill="{DynamicResource FgMuted}"
                          Data="{StaticResource Icon.Warning}" VerticalAlignment="Center"/>
                    <TextBlock Text="Durum (Stokta / Kritik / Tükendi) stok adedine göre otomatik belirlenir."
                               Style="{StaticResource Muted}" Margin="9,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
            <Button Style="{StaticResource SecondaryButton}" Content="İptal" Width="100" Click="Cancel_Click"/>
            <Button Style="{StaticResource PrimaryButton}" Content="Kaydet" Width="120" Margin="10,0,0,0" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
