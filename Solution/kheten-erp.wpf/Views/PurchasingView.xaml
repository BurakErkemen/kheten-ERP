<UserControl x:Class="kheten_erp.wpf.Views.PurchasingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBg}">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Satınalma" Style="{StaticResource H1}"/>
                <TextBlock Text="Tedarikçi siparişleri ve alış faturaları" Style="{StaticResource Muted}" Margin="0,3,0,0"/>
            </StackPanel>
            <Button Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="AddPurchase_Click">
                <StackPanel Orientation="Horizontal">
                    <Path Width="15" Height="15" Stretch="Uniform" Fill="White" Data="{StaticResource Icon.Add}" VerticalAlignment="Center"/>
                    <TextBlock Text="Yeni Sipariş" Margin="7,0,0,0" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Özet -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,16">
            <Border Style="{StaticResource Card}" Margin="0,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Bu Ay Alış" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 177.450" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Teslim Bekleyen" Style="{StaticResource Muted}"/>
                    <TextBlock Text="3" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Warning}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,0,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Ödenecek Borç" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 55.400" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Danger}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Tablo -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                      CellStyle="{StaticResource CellStyle}" RowStyle="{StaticResource RowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="BELGE NO" Binding="{Binding No}" Width="160"/>
                    <DataGridTextColumn Header="TEDARİKÇİ" Binding="{Binding Supplier}" Width="*"/>
                    <DataGridTextColumn Header="TARİH" Binding="{Binding Date}" Width="120"/>
                    <DataGridTextColumn Header="TUTAR" Binding="{Binding Amount}" Width="130"/>
                    <DataGridTemplateColumn Header="DURUM" Width="160" CellTemplate="{StaticResource StatusBadgeTemplate}"/>
                    <DataGridTemplateColumn Header="İŞLEMLER" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource GridIconButton}" Click="EditPurchase_Click" ToolTip="Düzenle">
                                        <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource FgMuted}" Data="{StaticResource Icon.Edit}"/>
                                    </Button>
                                    <Button Style="{StaticResource GridIconButton}" Click="DeletePurchase_Click" ToolTip="Sil" Margin="2,0,0,0">
                                        <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource Danger}" Data="{StaticResource Icon.Delete}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
