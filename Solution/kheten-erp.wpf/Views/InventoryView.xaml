<UserControl x:Class="kheten_erp.wpf.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             Background="{DynamicResource AppBg}">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Stok / Envanter" Style="{StaticResource H1}"/>
                <TextBlock Text="Ürün kartları, stok seviyeleri ve fiyatlar" Style="{StaticResource Muted}" Margin="0,3,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Style="{StaticResource SecondaryButton}" Content="Dışa Aktar"/>
                <Button Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Click="AddProduct_Click">
                    <StackPanel Orientation="Horizontal">
                        <Path Width="15" Height="15" Stretch="Uniform" Fill="White" Data="{StaticResource Icon.Add}" VerticalAlignment="Center"/>
                        <TextBlock Text="Yeni Ürün" Margin="7,0,0,0" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Mini özet -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <Border Style="{StaticResource Card}" Margin="0,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Toplam Ürün" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="TotalText" Text="0" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Stok Değeri" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 3.84M" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Kritik Seviye" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="CriticalText" Text="0" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Warning}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,0,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Tükenen" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="OutText" Text="0" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Danger}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Araç çubuğu (çalışır arama + filtre) -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <Border Width="300" Height="38" CornerRadius="8" Background="{DynamicResource Surface}"
                        BorderBrush="{DynamicResource Border}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Width="16" Height="16" Margin="11,0,0,0" Stretch="Uniform" VerticalAlignment="Center"
                              Fill="{DynamicResource FgMuted}" Data="{StaticResource Icon.Search}"/>
                        <Grid Grid.Column="1">
                            <TextBox x:Name="SearchBox" BorderThickness="0" Background="Transparent"
                                     Foreground="{DynamicResource Fg}" CaretBrush="{DynamicResource Fg}"
                                     FontSize="13" VerticalContentAlignment="Center" Margin="6,0,8,0"
                                     TextChanged="Filter_Changed"/>
                            <TextBlock Text="Ürün adı veya kodu ara" IsHitTestVisible="False"
                                       Foreground="{DynamicResource FgMuted}" FontSize="13"
                                       VerticalAlignment="Center" Margin="8,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Border>

                <ComboBox x:Name="CategoryFilter" Style="{StaticResource Combo}" Width="190" Margin="10,0,0,0"
                          SelectedIndex="0" SelectionChanged="Filter_Changed">
                    <sys:String>Kategori: Tümü</sys:String>
                    <sys:String>Hidrolik</sys:String>
                    <sys:String>Metal</sys:String>
                    <sys:String>Bağlantı</sys:String>
                    <sys:String>Elektrik</sys:String>
                    <sys:String>Yedek</sys:String>
                    <sys:String>Tesisat</sys:String>
                    <sys:String>Genel</sys:String>
                </ComboBox>

                <TextBlock x:Name="ResultCount" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="14,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Tablo -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                      CellStyle="{StaticResource CellStyle}" RowStyle="{StaticResource RowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="KOD" Binding="{Binding Code}" Width="110"/>
                    <DataGridTextColumn Header="ÜRÜN ADI" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="KATEGORİ" Binding="{Binding Category}" Width="130"/>
                    <DataGridTextColumn Header="BİRİM" Binding="{Binding Unit}" Width="80"/>
                    <DataGridTextColumn Header="STOK" Binding="{Binding Stock}" Width="90"/>
                    <DataGridTextColumn Header="BİRİM FİYAT" Binding="{Binding Price}" Width="120"/>
                    <DataGridTemplateColumn Header="DURUM" Width="120" CellTemplate="{StaticResource StatusBadgeTemplate}"/>
                    <DataGridTemplateColumn Header="İŞLEMLER" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource GridIconButton}" Click="EditProduct_Click" ToolTip="Düzenle">
                                        <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource FgMuted}" Data="{StaticResource Icon.Edit}"/>
                                    </Button>
                                    <Button Style="{StaticResource GridIconButton}" Click="DeleteProduct_Click" ToolTip="Sil" Margin="2,0,0,0">
                                        <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource Danger}" Data="{StaticResource Icon.Delete}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
