<UserControl x:Class="kheten_erp.wpf.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBg}">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Satış" Style="{StaticResource H1}"/>
                <TextBlock Text="Satış faturaları, siparişler ve tahsilat takibi" Style="{StaticResource Muted}" Margin="0,3,0,0"/>
            </StackPanel>
            <Button Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="AddSale_Click">
                <StackPanel Orientation="Horizontal">
                    <Path Width="15" Height="15" Stretch="Uniform" Fill="White" Data="{StaticResource Icon.Add}" VerticalAlignment="Center"/>
                    <TextBlock Text="Yeni Fatura" Margin="7,0,0,0" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Özet -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,16">
            <Border Style="{StaticResource Card}" Margin="0,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Bu Ay Toplam Satış" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 842.560" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Tahsil Edilen" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 718.260" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Success}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,0,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Bekleyen + Geciken" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 124.300" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Warning}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Çalışan durum sekmeleri -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <RadioButton x:Name="TabAll"     Style="{StaticResource FilterTab}" GroupName="SalesTab" Content="Tümü" IsChecked="True" Checked="Tab_Checked"/>
            <RadioButton x:Name="TabPaid"    Style="{StaticResource FilterTab}" GroupName="SalesTab" Content="Ödendi" Checked="Tab_Checked"/>
            <RadioButton x:Name="TabPending" Style="{StaticResource FilterTab}" GroupName="SalesTab" Content="Bekliyor" Checked="Tab_Checked"/>
            <RadioButton x:Name="TabLate"    Style="{StaticResource FilterTab}" GroupName="SalesTab" Content="Gecikti" Checked="Tab_Checked"/>
            <TextBlock x:Name="ResultCount" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Tablo -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                      CellStyle="{StaticResource CellStyle}" RowStyle="{StaticResource RowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FATURA NO" Binding="{Binding No}" Width="160"/>
                    <DataGridTextColumn Header="MÜŞTERİ" Binding="{Binding Customer}" Width="*"/>
                    <DataGridTextColumn Header="TARİH" Binding="{Binding Date}" Width="120"/>
                    <DataGridTextColumn Header="TUTAR" Binding="{Binding Amount}" Width="130"/>
                    <DataGridTemplateColumn Header="DURUM" Width="130" CellTemplate="{StaticResource StatusBadgeTemplate}"/>
                    <DataGridTemplateColumn Header="İŞLEMLER" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource GridIconButton}" Click="EditSale_Click" ToolTip="Düzenle">
                                        <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource FgMuted}" Data="{StaticResource Icon.Edit}"/>
                                    </Button>
                                    <Button Style="{StaticResource GridIconButton}" Click="DeleteSale_Click" ToolTip="Sil" Margin="2,0,0,0">
                                        <Path Width="16" Height="16" Stretch="Uniform" Fill="{DynamicResource Danger}" Data="{StaticResource Icon.Delete}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
