<UserControl x:Class="kheten_erp.wpf.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBg}">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Satış" Style="{StaticResource H1}"/>
                <TextBlock Text="Satış faturaları, siparişler ve tahsilat takibi" Style="{StaticResource Muted}" Margin="0,3,0,0"/>
            </StackPanel>
            <Button Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <Path Width="15" Height="15" Stretch="Uniform" Fill="White" Data="{StaticResource Icon.Add}" VerticalAlignment="Center"/>
                    <TextBlock Text="Yeni Fatura" Margin="7,0,0,0" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Özet -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,16">
            <Border Style="{StaticResource Card}" Margin="0,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Bu Ay Toplam Satış" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 842.560" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,9,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Tahsil Edilen" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 718.260" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Success}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="9,0,0,0" Padding="16">
                <StackPanel>
                    <TextBlock Text="Bekleyen + Geciken" Style="{StaticResource Muted}"/>
                    <TextBlock Text="₺ 124.300" Style="{StaticResource MetricValue}" FontSize="22" Margin="0,4,0,0" Foreground="{DynamicResource Warning}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Sekme benzeri filtre -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <Border CornerRadius="8" Background="{DynamicResource Primary}" Padding="14,7" Margin="0,0,8,0">
                <TextBlock Text="Tümü" Foreground="White" FontWeight="SemiBold" FontSize="12.5"/>
            </Border>
            <Border CornerRadius="8" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="1" Padding="14,7" Margin="0,0,8,0">
                <TextBlock Text="Ödendi" Foreground="{DynamicResource FgMuted}" FontWeight="SemiBold" FontSize="12.5"/>
            </Border>
            <Border CornerRadius="8" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="1" Padding="14,7" Margin="0,0,8,0">
                <TextBlock Text="Bekliyor" Foreground="{DynamicResource FgMuted}" FontWeight="SemiBold" FontSize="12.5"/>
            </Border>
            <Border CornerRadius="8" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="1" Padding="14,7">
                <TextBlock Text="Gecikti" Foreground="{DynamicResource FgMuted}" FontWeight="SemiBold" FontSize="12.5"/>
            </Border>
        </StackPanel>

        <!-- Tablo -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                      CellStyle="{StaticResource CellStyle}" RowStyle="{StaticResource RowStyle}"
                      ItemsSource="{Binding Sales}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FATURA NO" Binding="{Binding No}" Width="160"/>
                    <DataGridTextColumn Header="MÜŞTERİ" Binding="{Binding Customer}" Width="*"/>
                    <DataGridTextColumn Header="TARİH" Binding="{Binding Date}" Width="130"/>
                    <DataGridTextColumn Header="TUTAR" Binding="{Binding Amount}" Width="140"/>
                    <DataGridTemplateColumn Header="DURUM" Width="140" CellTemplate="{StaticResource StatusBadgeTemplate}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
